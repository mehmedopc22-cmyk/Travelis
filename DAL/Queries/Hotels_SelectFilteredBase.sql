SELECT
    h.Id,
    h.Name,
    h.Country,
    h.City,
    h.Street,
    h.PostalCode,
    h.PhoneNumber,
    h.Email,
    h.Status,
    h.Approved,
    h.CreatedAt,
    h.UpdatedAt
FROM Hotels h
WHERE 1 = 1
